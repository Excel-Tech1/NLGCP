# Research Workspace

Use `notebooks/` for exploration, `experiments/` for reproducible configurations/code, and `validation/` for evidence and reports. Do not embed large datasets in notebooks or Git. Every scientific result must identify input checksums, source, reference frame, software version, configuration, and repeatable command. Phase 1 contains no GNSS algorithms or results.
