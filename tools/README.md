# External GNSS Tools

RTKLIB and PRIDE PPP-AR are explicit external dependencies. They are not vendored because an arbitrary or modified upstream snapshot would undermine reproducibility. Their status is reported by `make health`.
