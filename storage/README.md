# Runtime Storage

This tree describes local mount points for RINEX, RTCM, navigation messages, precise products, and logs. Contents are ignored by Git. Raw incoming files are append-only/immutable; processing writes new derived objects. Production storage will use access-controlled object storage with integrity metadata, lifecycle rules, backups, and provider-agreement enforcement.
